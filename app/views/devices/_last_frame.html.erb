<%# The frame a panel was last sent, as the panel shows it. Loading it
    records no check-in (Devices::LastFramesController). %>
<%= render "application/panel_picture", src: (device_last_frame_path(device) if framed),
           alt: "The last frame sent to #{device.name}",
           width: device.width, height: device.height, classes: %>

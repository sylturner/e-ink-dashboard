<% if events.empty? %>
  <div class="t-xs"><%= empty %></div>
<% else %>
  <ul class="rows">
    <%= render partial: "renders/items/calendar/event", collection: events, as: :event, locals: { tags: tags, times: times } %>
  </ul>
<% end %>

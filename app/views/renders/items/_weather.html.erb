<% case item.view %>
<% when "hourly" %>
  <div class="hours">
    <% (data["hourly"] || []).first(item.setting("hour_count").to_i).each do |h| %>
      <div class="hour">
        <div class="t-sm"><%= h["hour"] %></div>
        <%= weather_icon(h["icon"], scale: 1) %>
        <div class="t-sm"><%= h["temp"] %>&deg;</div>
      </div>
    <% end %>
  </div>

<% when "forecast" %>
  <ul class="rows">
    <% (data["daily"] || []).first(item.setting("day_count").to_i).each do |d| %>
      <li>
        <span class="t-sm"><%= d["day"] %></span>
        <span><%= weather_icon(d["icon"], scale: 1) %></span>
        <span class="t-sm"><%= d["high"] %>&deg; / <%= d["low"] %>&deg;</span>
      </li>
    <% end %>
  </ul>

<% else %>
  <% c = data["current"] || {} %>
  <div class="now">
    <%= weather_icon(c["icon"], scale: 3) %>
    <div>
      <div class="t-lg"><%= c["temp"] %>&deg;</div>
      <div class="t-sm"><%= c["label"] %></div>
      <div class="t-sm">Feels <%= c["feels_like"] %>&deg;</div>
    </div>
  </div>
  <% today = (data["daily"] || []).first %>
  <% if today %>
    <div class="t-sm">
      H <%= today["high"] %>&deg; &middot; L <%= today["low"] %>&deg;
      <%= "· #{today["precip"]}%" if today["precip"].to_i.positive? %>
    </div>
  <% end %>
<% end %>

<%
  fetched = item.sources.filter_map(&:fetched_at).min
  failing = item.sources.any? { |s| s.failure_count.to_i > 2 }
%>
<% if fetched.present? %>
  <div class="card-footer">
    <%= fetched.in_time_zone(now.time_zone).strftime("%-l:%M %p") %><%= " · stale" if failing %>
  </div>
<% end %>

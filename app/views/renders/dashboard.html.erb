<div class="grid"
     style="--grid-cols: <%= @dashboard.grid_columns %>;
            --grid-rows: <%= @dashboard.grid_rows %>;">
  <% @items.each do |item| %>
    <%= render "renders/item", item: item, now: @now %>
  <% end %>
</div>

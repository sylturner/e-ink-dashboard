<% content_for :title, "Add #{Source::PROVIDER_LABELS[@type]}" %>

<h1>Add <%= Source::PROVIDER_LABELS[@type] %></h1>

<%= render "form", source: @source, type: @type %>

<p><%= link_to "Back to sources", sources_path %></p>

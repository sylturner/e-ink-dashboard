<% content_for :title, "Add #{Source.provider_label(@type)}" %>

<h1>Add <%= Source.provider_label(@type) %></h1>

<%= render "form", source: @source, type: @type %>

<p><%= link_to "Back to sources", sources_path %></p>

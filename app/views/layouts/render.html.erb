<!DOCTYPE html>
<html data-theme="<%= @dashboard&.theme || "default" %>"
      style="--screen-w: <%= @device&.width || 800 %>px;
             --screen-h: <%= @device&.height || 480 %>px;">
  <head>
    <meta charset="utf-8">
    <link rel="icon" href="data:,">
    <style><%= raw InlineAssets.css("render.css") %></style>
  </head>
  <body><%= yield %></body>
</html>
